import localFont from "next/font/local"

const PP_Monument_Variable = localFont({
  src: "./PPMonumentExtended-Variable.woff2",
  fallback: ["sans-serif"],
})

const Satoshi = localFont({
  src: "./Satoshi-Variable.woff2",
  fallback: ["sans-serif"],
})

export { PP_Monument_Variable, Satoshi }
